export class AppPhongThuyRespone {
    can: string;
    chi: string;
    id: number;

    constructor(can: string, chi: string, id: number){
        this.can = can;
        this.chi = chi;
        this.id = id;
    }
}